# SDK Coordinates

- Domain: ai
- Language: python
- SDK package directory: `sdkwork-ai-sdk-python`
- Build descriptor: `pyproject.toml`
- SDK coordinate: `sdkwork-ai-sdk==1.0.0`

## Integration Commands

- Registry mode: `pip install sdkwork-ai-sdk==1.0.0`
- Local mode: `pip install <path-to-sdkwork-ai-sdk-python>`

## Validation Commands

- Stage 1: `python -m pip show sdkwork-ai-sdk`
- Stage 5: `python -m pytest`

